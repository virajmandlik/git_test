"use strict" //strictly follow newer js version

// alert(3 +3)  // we ae using the node js nand not the browsers

console.log('hello')

let a= 45
let b = "hello"
console.table([typeof(a),typeof(b)])
console.table([typeof(null),typeof(undefined)]) 